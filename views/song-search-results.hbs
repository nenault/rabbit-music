<h1>Song result</h1>

{{#each results}}
<p>{{this.name}} de {{#each this.artists}}{{this.name}}, {{/each}}</p>
<audio controls src="{{this.preview_url}}">
    Your browser does not support the
    <code>audio</code> element.
</audio>
{{/each}}